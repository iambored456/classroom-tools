The live app source now lives here as a monorepo multi-page Vite app.

Preserved supporting files:

- `legacy/`: backup CSS/JS and the old standalone build helper.
- `notes/`: design/spec material from the original widget folder.
